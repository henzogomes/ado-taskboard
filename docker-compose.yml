services:
  taskboard:
    build:
      context: .
    # No ADO config: the container just serves the app. Users connect from the
    # in-app login screen (org/project/PAT stored in their browser). PORT is the
    # only runtime env, defaulted below.
    environment:
      PORT: 5280
    ports:
      - '5280:5280'
